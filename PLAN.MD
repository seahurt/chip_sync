# SeqSync Windows 生产级软件说明书

## 1. 软件概述

**软件名称**：SeqSync Windows  
**软件定位**：测序数据（芯片目录）自动同步工具  
**运行平台**：Windows 7 / Windows 10 / Windows 11
**目标用户**：测序平台运维人员、实验室 IT 管理员

SeqSync Windows 是一款面向生产环境设计的 Windows 桌面同步工具，用于将本地测序芯片数据目录可靠、自动地同步至远程服务器。软件以稳定性、可恢复性和长期无人值守运行为首要设计目标。

远端服务器采用 **rsync daemon 模式**，本地通过内置的 rsync 客户端执行增量同步，确保在大规模文件与超大文件场景下的高效与安全。

---

## 2. 设计目标与原则

1. **生产级稳定性**：支持 7×24 小时运行，避免资源泄漏和僵尸进程。
2. **强一致性保障**：避免并发同步导致的数据竞争与覆盖风险。
3. **可恢复能力**：面对网络中断、服务器异常时可自动恢复。
4. **可维护性**：提供清晰日志、明确错误信息，便于排障。
5. **低运维成本**：单一可执行文件，无需复杂环境配置。

---

## 3. 核心功能

### 3.1 本地目录同步

- 用户选择一个“芯片父目录”作为同步源。
- 程序自动识别并同步其下的子目录（芯片目录）。
- 支持 rsync 增量同步，仅传输发生变化的文件内容。

### 3.2 定时同步机制

- 默认同步周期：5 分钟（可配置）。
- 使用内部调度器定时触发同步任务。
- 若当前同步尚未完成，不启动新的并发同步任务。

### 3.3 任务合并策略（生产级优化）

- 若在同步过程中检测到新的目录变更：

  - 不跳过任务
  - 标记为 **Dirty 状态**
  - 当前同步完成后立即再执行一次同步

### 3.4 并发与锁控制

- 全局互斥锁保证同一时间仅有一个 rsync 进程运行。
- 防止带宽占用失控或服务器 I/O 压力过载。

### 3.5 日志系统

- 本地日志文件记录：

  - 同步开始 / 结束时间
  - rsync 执行参数
  - 错误码与错误信息

- 支持日志轮转（按大小或日期）。

---

## 4. 软件运行流程

### 4.1 初始化阶段

1. 加载配置文件。配置文件中包括本地 rsync 执行路径、远程 rsync 地址（用户名、密码、模块）、芯片父目录、同步周期等参数。
2. 校验本地 rsync 可执行文件是否存在。

### 4.2 定时触发流程

1. 定时器触发同步检查。
2. 判断当前是否存在正在运行的同步任务：

   - 若存在：标记 Dirty 状态，结束本次触发。
   - 若不存在：进入同步流程。

### 4.3 同步执行阶段

- 构建 rsync 命令，例如：

```
rsync -avz --partial --inplace <local_path> <remote_module>
```

- 使用独立进程执行 rsync。
- 监听进程退出状态与返回码。

### 4.4 同步结束处理

- 若同步成功：

  - 清除 Dirty 状态

- 若同步失败：

  - 记录错误日志
  - 保留 Dirty 状态，等待下一个周期重试

---

## 5. 异常处理与恢复策略

### 5.1 网络异常

- rsync 返回网络错误时：

  - 记录错误
  - 不中断程序运行
  - 下一个周期自动重试

### 5.2 服务器异常

- daemon 拒绝连接 / 模块不存在：

  - 标记为配置错误
  - 在 UI 中提示用户

### 5.3 本地异常

- 磁盘空间不足：

  - 停止同步
  - 输出明确错误提示

---

## 6. 安全与权限设计

1. rsync daemon 认证信息：

   - 密码文件存储在本地配置目录
   - 不在日志中明文输出

2. 不在 UI 中展示敏感信息（密码、token）。
3. 明确建议服务器端限制：

   - max connections
   - bandwidth limit

---

## 7. 技术架构与技术栈选择

### 7.1 技术栈

- **语言**：Go
- **UI 框架**：Wails
- **进程管理**：os/exec + context.Context
- **并发控制**：sync.Mutex / atomic
- **日志系统**：zap / slog

### 7.2 选择 Go + Wails 的原因

1. 原生并发与进程管理能力，适合长期运行任务。
2. 单一 exe 打包，部署简单，杀毒误报率低。
3. 对 rsync 等外部命令控制精细，可强制终止。
4. 前后端逻辑清晰分离，易维护、易扩展。

---

## 8. Windows 平台专项注意事项

1. **路径处理**：

   - 统一使用绝对路径
   - 处理空格、中文与超长路径

2. **rsync 行为注意**：

   - 建议启用 --partial / --inplace
   - 避免频繁删除与重建大文件

3. **杀毒软件兼容性**：

   - 建议对安装目录加入白名单

---

## 9. 可扩展方向（未来）

- Windows 服务模式运行
- 文件系统变更实时触发
- 多服务器同步目标支持
- 同步状态 Web API 输出

---

## 10. 总结

SeqSync Windows 并非简单的 rsync GUI 包装，而是为测序数据生产环境设计的稳定同步组件。通过 Go + Wails 架构与严格的运行控制策略，确保在高数据量、高可靠性要求下长期稳定运行。
